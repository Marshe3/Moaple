<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aischool.mapper.BoardMapper"> 
<!--  namespace => BoardMapper의 interface를 참고해서 사용하겠다, interface를 똑같이 사용해야 함 -->
	
	<!-- BoardMapper 안에 메소드가 동일한 아이디를 찾아간다, id가 일치해야 함, resultType => select는 돌려줄 값이 있다 -->
	<select id="boardList" resultType="com.aischool.entity.Board"> 
		SELECT * FROM community ORDER BY createdAt DESC
	</select>
	
	<insert id="boardInsert" parameterType="com.aischool.entity.Board" >
		INSERT INTO community(title, content, userId, filepath) VALUES(#{title}, #{content}, #{userId}, #{filepath})
	</insert>
	
	<select id="boardContents" parameterType="int" resultType="com.aischool.entity.Board">
		SELECT * FROM community WHERE communityId = #{communityId}
	</select>
	
	<update id="boardUpdate" parameterType="com.aischool.entity.Board">
		UPDATE community SET title = #{title}, content = #{content}, userId = #{userId}, filepath = #{filepath} WHERE communityId = #{communityId}
	</update>
	
	<delete id="boardDelete" parameterType="int">
		DELETE FROM community WHERE communityId = #{communityId}
	</delete>
	
	<update id="boardCount" parameterType="int">
		UPDATE community SET viewCount = viewCount + 1 WHERE communityId = #{communityId}
	</update>
	
	<!-- 총 게시글 수 -->
	<select id="boardTotalCount" resultType="int">
	    SELECT COUNT(*) FROM community
	</select>
	
	<!-- 페이징 목록 (최신순) -->
   <select id="boardListPaged" resultType="com.aischool.entity.Board">
    	SELECT communityId, title, content, userId, createdAt, viewCount, filepath FROM community ORDER BY communityId DESC LIMIT #{limit} OFFSET #{offset}
   </select>

</mapper>